import 'package:flutter/widgets.dart';

const padding = 8.0;
const edgeInsets = EdgeInsets.all(padding);
const vSpacer = SizedBox(height: padding);
const hSpacer = SizedBox(width: padding);

const padding2 = 16.0;
const edgeInsets2 = EdgeInsets.all(padding2);
const vSpacer2 = SizedBox(height: padding2);
const hSpacer2 = SizedBox(width: padding2);

const radius = 20.0;
const cardBorderRadius = BorderRadius.all(Radius.circular(radius));
